diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index 74ffbd5..02b2678 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -39,8 +39,10 @@ jobs:
     env:
       # PrismaClient membaca DATABASE_URL saat konstruksi (sitemap.ts
       # di-prerender saat build). Test unit memakai mock penuh, jadi DB
-      # dummy cukup — tidak ada koneksi nyata yang dibutuhkan.
-      DATABASE_URL: "file:./ci.db"
+      # dummy cukup — tidak ada koneksi nyata yang dibutuhkan (query
+      # prerender gagal tertangani try/catch di sitemap). Skema tunggal
+      # PostgreSQL — dummy URL harus berprotokol postgresql://.
+      DATABASE_URL: "postgresql://ci:ci@localhost:5432/ci_dummy"
       # AUTH_SECRET tidak wajib saat build, tapi diset untuk menghindari
       # peringatan dari src/lib/auth.ts. Ganti dengan secret asli di
       # production (jangan pernah commit secret di sini).
@@ -66,8 +68,9 @@ jobs:
       - name: Lint (Markdown)
         run: bun run lint:md
 
-      - name: Check schema sync
-        run: bun run check:schema
+      # (Check schema sync DIHAPUS 2026-08-28 — skema tunggal PostgreSQL,
+      # tidak ada lagi pasangan schema.prisma/schema.postgres.prisma yang
+      # perlu dijaga sinkron.)
 
       # Guard klasifikasi scope publik (public-scope.test.ts): setiap kolom
       # model Teacher/OrgStructure harus terklasifikasi sadar (daftar PUTIH
@@ -98,11 +101,26 @@ jobs:
     permissions:
       contents: read
       pull-requests: write
+    # PostgreSQL service container — skema tunggal PostgreSQL; e2e butuh DB
+    # nyata berisi seed (admin/operator/guru, berita, pengumuman, agenda).
+    services:
+      postgres:
+        image: postgres:16-alpine
+        env:
+          POSTGRES_USER: postgres
+          POSTGRES_PASSWORD: postgres
+          POSTGRES_DB: ci_e2e
+        ports:
+          - 5432:5432
+        options: >-
+          --health-cmd "pg_isready -U postgres"
+          --health-interval 10s
+          --health-timeout 5s
+          --health-retries 5
     env:
-      # E2E butuh database SQLite berisi seed (admin/operator/guru, berita,
-      # pengumuman, agenda) — seed dilewati bila DB sudah terisi, jadi
-      # db:push lalu db:seed cukup untuk database baru per run.
-      DATABASE_URL: "file:./ci-e2e.db"
+      # DB e2e: service container postgres di atas (bukan secret — data CI
+      # sekali pakai). db:push + db:seed cukup untuk database baru per run.
+      DATABASE_URL: "postgresql://postgres:postgres@localhost:5432/ci_e2e?schema=public"
       AUTH_SECRET: "ci-insecure-build-secret"
       NEXT_PUBLIC_SITE_URL: "https://sdn-mongisidi1.sch.id"
     steps:
@@ -319,17 +337,22 @@ jobs:
       - name: Check test data leak
         run: bun run check:data-leak
 
-      # Stage state untuk job orphan-check: DB SQLite e2e + file upload BOS.
-      # Hanya file bos-*.pdf yang disalin (bukan seluruh public/uploads)
-      # agar artifact kecil — check hanya peduli file BOS. Di-upload di
-      # SEMUA run (if: always()) konsisten dengan server log; job
-      # orphan-check sendiri hanya berjalan bila suite HIJAU (needs: e2e),
-      # jadi kegagalan suite tidak menambah noise "yatim" di atas merah.
+      # Stage state untuk job orphan-check: dump PostgreSQL e2e + file
+      # upload BOS. Hanya file bos-*.pdf yang disalin (bukan seluruh
+      # public/uploads) agar artifact kecil — check hanya peduli file BOS.
+      # Dump memakai container postgres:16-alpine (pg_dump versi cocok,
+      # terpasang via docker runner — bukan pg_dump bawaan runner).
+      # Di-upload di SEMUA run (if: always()) konsisten dengan server log;
+      # job orphan-check sendiri hanya berjalan bila suite HIJAU
+      # (needs: e2e), jadi kegagalan suite tidak menambah noise "yatim".
       - name: Stage e2e state for orphan check
         if: always()
         run: |
-          mkdir -p orphan-state/prisma orphan-state/uploads
-          cp prisma/ci-e2e.db* orphan-state/prisma/ 2>/dev/null || true
+          mkdir -p orphan-state/uploads
+          docker run --rm --network host -e PGPASSWORD=postgres postgres:16-alpine \
+            pg_dump -h 127.0.0.1 -U postgres -d ci_e2e --no-owner --no-privileges \
+            > orphan-state/db.sql \
+            || echo "pg_dump gagal — orphan-check akan melihat DB kosong"
           cp public/uploads/bos-*.pdf orphan-state/uploads/ 2>/dev/null || true
 
       - name: Upload e2e orphan state
@@ -388,10 +411,26 @@ jobs:
     name: Check orphaned BOS files
     needs: e2e
     runs-on: ubuntu-latest
+    # PostgreSQL service container — DB di-restore dari dump artifact e2e
+    # (berisi baris BosDocument hasil suite) via psql di dalam container.
+    services:
+      postgres:
+        image: postgres:16-alpine
+        env:
+          POSTGRES_USER: postgres
+          POSTGRES_PASSWORD: postgres
+          POSTGRES_DB: ci_e2e
+        ports:
+          - 5432:5432
+        options: >-
+          --health-cmd "pg_isready -U postgres"
+          --health-interval 10s
+          --health-timeout 5s
+          --health-retries 5
     env:
-      # PrismaClient membaca DATABASE_URL saat konstruksi; DB SQLite yang
-      # di-restore dari artifact e2e (berisi baris BosDocument hasil suite).
-      DATABASE_URL: "file:./ci-e2e.db"
+      # PrismaClient membaca DATABASE_URL saat konstruksi; DB yang
+      # di-restore dari dump artifact e2e ke service container di atas.
+      DATABASE_URL: "postgresql://postgres:postgres@localhost:5432/ci_e2e?schema=public"
       AUTH_SECRET: "ci-insecure-build-secret"
       NEXT_PUBLIC_SITE_URL: "https://sdn-mongisidi1.sch.id"
     steps:
@@ -411,15 +450,17 @@ jobs:
           name: e2e-orphan-state
           path: orphan-state
 
-      # Pulihkan DB + uploads ke lokasi yang diharapkan skrip
-      # (prisma/ci-e2e.db relatif ke schema; public/uploads), lalu jalankan
+      # Pulihkan DB (dump artifact → service container postgres) + uploads
+      # ke lokasi yang diharapkan skrip (public/uploads), lalu jalankan
       # check. Skrip juga melaporkan BARIS BosDocument yang file-nya hilang
       # (data-integrity) — exit 1 bila jumlah baris gantung melebihi
       # ROWS_WITHOUT_FILE_EXPECT. Kedua knob bisa di-override via
       # workflow_dispatch bila suatu saat ada baseline yang disengaja.
       - name: Run orphan check
         run: |
-          mkdir -p prisma public/uploads
-          cp orphan-state/prisma/ci-e2e.db* prisma/ 2>/dev/null || true
+          mkdir -p public/uploads
           cp orphan-state/uploads/* public/uploads/ 2>/dev/null || true
+          docker run --rm -i --network host -e PGPASSWORD=postgres postgres:16-alpine \
+            psql -h 127.0.0.1 -U postgres -d ci_e2e -v ON_ERROR_STOP=0 \
+            < orphan-state/db.sql
           bun run clean:orphans -- --check
diff --git a/.github/workflows/deploy-vercel.yml b/.github/workflows/deploy-vercel.yml
index fd8d57d..825003b 100644
--- a/.github/workflows/deploy-vercel.yml
+++ b/.github/workflows/deploy-vercel.yml
@@ -52,6 +52,7 @@ jobs:
       # `bash scripts/...` (bukan ./scripts/...) agar tidak bergantung pada
       # executable bit di git — aman dari repo yang di-commit di Windows.
       # Script menjalankan: prisma generate → migrate deploy → seed (semua
-      # memakai schema.postgres.prisma). Seed idempotent (skip bila DB terisi).
+      # memakai skema tunggal prisma/schema.prisma — PostgreSQL). Seed
+      # idempotent (skip bila DB terisi).
       - name: Run deploy script (migrate + seed)
         run: bash scripts/deploy-vercel.sh
diff --git a/.github/workflows/playwright.yml b/.github/workflows/playwright.yml
index 6ec72c3..4834258 100644
--- a/.github/workflows/playwright.yml
+++ b/.github/workflows/playwright.yml
@@ -43,11 +43,26 @@ jobs:
     permissions:
       contents: read
       pull-requests: write
+    # PostgreSQL service container — skema tunggal PostgreSQL; e2e butuh DB
+    # nyata berisi seed, dan build membaca DB saat prerender sitemap.
+    services:
+      postgres:
+        image: postgres:16-alpine
+        env:
+          POSTGRES_USER: postgres
+          POSTGRES_PASSWORD: postgres
+          POSTGRES_DB: ci_e2e_prod
+        ports:
+          - 5432:5432
+        options: >-
+          --health-cmd "pg_isready -U postgres"
+          --health-interval 10s
+          --health-timeout 5s
+          --health-retries 5
     env:
-      # E2E butuh database SQLite berisi seed (admin/operator/guru, berita,
-      # pengumuman, agenda), dan build juga membaca DB saat prerender
-      # sitemap — jadi db:push + db:seed harus selesai sebelum build.
-      DATABASE_URL: "file:./ci-e2e-prod.db"
+      # DB e2e: service container postgres di atas (bukan secret — data CI
+      # sekali pakai). db:push + db:seed harus selesai sebelum build.
+      DATABASE_URL: "postgresql://postgres:postgres@localhost:5432/ci_e2e_prod?schema=public"
       # Bukan secret asli — cukup untuk build & test. Ganti dengan secret
       # asli di production (jangan pernah commit secret di sini).
       AUTH_SECRET: "ci-insecure-build-secret"
